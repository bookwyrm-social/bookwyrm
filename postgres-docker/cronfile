SHELL=/bin/bash
MAIlTO=

# We output things to /proc/1/fd/1 as cron is pid 1 and we want to log
# script output to container stdout
0 0 * * * root /usr/local/bin/bookwyrm-backup.sh > /proc/1/fd/1 2>&1
# If uncommented, this script will weed the backups directory. It will keep the 14
# most-recent backups, then one backup/week for the next four backups, then one
# backup/month after that.
0 5 * * * root /usr/local/bin/bookwyrm-weed.sh -d 14 -w 4 -m -1 /backups > /proc/1/fd/1 2>&1
